<Window x:Class="KasseApp.Views.CartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Warenkorb"
        Height="420"
        Width="520"
        WindowStartupLocation="CenterOwner"
        Background="#FF0F172A"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <!-- Basis-Button -->
        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="#FF2563EB"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF1D4ED8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF1E40AF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#FF4B5563"/>
                                <Setter Property="Foreground" Value="#FF9CA3AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="#FF2563EB"/>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="#FF111827"/>
            <Setter Property="BorderBrush" Value="#FF374151"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="10"
            Background="#FF111827"
            BorderBrush="#FF1F2937"
            BorderThickness="1"
            Padding="0">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Titelzeile -->
            <Grid Grid.Row="0"
                  Background="#FF111827"
                  MouseDown="TitleBar_MouseDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="txtTitleBar"
                           Text="Warenkorb"
                           Foreground="White"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Margin="10,0,0,0"
                           VerticalAlignment="Center"/>

                <Button Content="✕"
                        Width="32"
                        Height="24"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Foreground="#F97373"
                        Grid.Column="1"
                        Margin="0,0,4,0"
                        Click="Close_Click"/>
            </Grid>

            <!-- Inhalt -->
            <Grid Grid.Row="1" Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- DataGrid -->
                <DataGrid x:Name="dgCart"
                          Grid.Row="0"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          Background="Transparent"
                          BorderThickness="0"
                          Foreground="White"
                          AlternatingRowBackground="#FF111827"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column">
                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <ContextMenu.Resources>
                                <!-- Dark-Theme für Kontextmenü -->
                                <Style TargetType="ContextMenu">
                                    <Setter Property="Background" Value="#FF111827"/>
                                    <Setter Property="BorderBrush" Value="#FF1F2937"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                </Style>
                                <Style TargetType="MenuItem">
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Padding" Value="10,4"/>
                                    <Setter Property="FontSize" Value="13"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="MenuItem">
                                                <Border x:Name="border"
                                                        Background="{TemplateBinding Background}"
                                                        CornerRadius="4"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsHighlighted" Value="True">
                                                        <Setter TargetName="border" Property="Background" Value="#FF111827"/>
                                                    </Trigger>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter Property="Foreground" Value="#FF6B7280"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ContextMenu.Resources>

                            <MenuItem x:Name="miDeletePos"
                                      Header="Position löschen"
                                      Click="Menu_DeletePosition_Click"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#FF111827"/>
                            <Setter Property="Foreground" Value="#FF9CA3AF"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#FF374151"/>
                            <Setter Property="Padding" Value="8,0,8,0"/>
                        </Style>

                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="SnapsToDevicePixels" Value="True"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF111827"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#402563EB"/>
                                    <Setter Property="BorderBrush" Value="#FF2563EB"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn x:Name="colArtikel"
                                            Binding="{Binding Artikel.Name}"
                                            Width="2*"/>
                        <DataGridTextColumn x:Name="colMenge"
                                            Binding="{Binding Menge}"
                                            Width="*"/>
                        <DataGridTextColumn x:Name="colPreis"
                                            Binding="{Binding Artikel.Preis, StringFormat={}{0:0.00} €}"
                                            Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Gesamtsumme -->
                <TextBlock x:Name="txtTotal"
                           Grid.Row="1"
                           Margin="0,8,0,0"
                           HorizontalAlignment="Right"
                           Foreground="White"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Text="Summe: 0,00 €"/>
            </Grid>

            <!-- Buttons unten -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,0,10,10">
                <Button x:Name="btnClear"
                        Content="Leeren"
                        Width="110"
                        Margin="0,0,8,0"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Click="BtnClear_Click"/>
                <Button x:Name="btnPay"
                        Content="Bezahlen"
                        Width="120"
                        Margin="0,0,8,0"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="BtnPay_Click"/>
                <Button x:Name="btnCancel"
                        Content="Abbrechen"
                        Width="90"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Click="BtnCancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
